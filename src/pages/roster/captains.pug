extends ../layout

block content
	- t = "Captain"
	- fmembers = members.filter(m => m["What sub-teams are you a part of in robotics?"].includes(t))
		.card-columns.my-4.d-flex.flex-wrap
			each n in fmembers
				+member(n)
